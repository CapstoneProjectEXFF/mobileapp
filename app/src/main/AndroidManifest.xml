<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project.capstone.exchangesystem">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <!-- <activity android:name=".MainActivity"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->


        <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->
        <activity android:name=".Activity.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- <activity android:name=".WelcomeActivity" /> -->
        <activity android:name=".Activity.SignUpAcitivity" />
        <activity android:name=".Activity.SignInActivity" />
        <activity android:name=".Activity.ForgetPasswordActivity" />
        <activity android:name=".Activity.CreateSuccessActivity" />
        <activity android:name=".Activity.MainActivity" />
        <activity android:name=".Activity.VerifyActivity" />
        <activity android:name=".Activity.ProfileActivity" />
        <activity
            android:name=".Activity.SearchActivity"
            android:theme="@style/AppTheme2" />
        <activity android:name=".Activity.DescriptionItem" />
        <activity android:name=".Activity.TradeActivity" />
        <activity android:name=".Activity.CreateItemActivity"></activity>
        <activity android:name=".Activity.UpdateItemActivity"></activity>
        <activity android:name=".Activity.OwnInventory"></activity>
    </application>

</manifest>